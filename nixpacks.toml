[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --omit=dev --ignore-scripts || npm install --omit=dev --ignore-scripts"]

[phases.build]
cmds = ["npm run server:build"]

[start]
cmd = "npm run server:prod"
